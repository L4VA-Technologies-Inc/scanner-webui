import React from 'react';
import { Box, Heading } from '@chakra-ui/react';

const BlockchainQuery: React.FC = () => {
  return (
    <Box>
      <Heading size="lg" mb={4}>Blockchain Query</Heading>
      {/* Placeholder for blockchain query content */}
    </Box>
  );
};

export default BlockchainQuery;
