import React from 'react';
import { Box, Heading } from '@chakra-ui/react';

const WebhookActivity: React.FC = () => {
  return (
    <Box>
      <Heading size="lg" mb={4}>Webhook Activity</Heading>
      {/* Placeholder for webhook activity content */}
    </Box>
  );
};

export default WebhookActivity;
